# Changelog

このプロジェクトの変更履歴。[Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) の形式に準拠。

## [1.6.0] - 2026-09-07

### 追加

- デスクトップ版のプロジェクト作成・保存先変更で、「ローカルストレージ」を選んだときにOS純正のフォルダ選択ダイアログでPC内の好きな場所を保存先に指定できるように（これまではアプリ標準フォルダに固定で、選択肢が表示されていなかった）。

## [1.5.5] - 2026-09-07

### 修正

- デスクトップ版で、v1.5.4の修正後も内部バックエンドとの通信がブロックされ、ログイン画面が表示されてしまっていた不具合を修正。デスクトップ版のフロントエンド（`app://`）から内蔵バックエンド（`http://127.0.0.1:xxxx`）への通信はオリジンが異なるCookie付きリクエストになるが、ブラウザのCORS仕様上、サーバーがオリジンをワイルドカード（`*`）で許可している場合はCookie付きリクエストを受け付けられない制約があり、これに阻まれていた。デスクトップ版はもともとCookieを使わない固定ローカルユーザーで動作するため、この通信ではCookieを送らないように変更して解消。

## [1.5.4] - 2026-09-07

### 修正

- デスクトップ版（Windows）で、v1.5.3の修正後もなお発生していた起動失敗（`AttributeError: 'NoneType' object has no attribute 'isatty'` / `Unable to configure formatter 'default'`）を修正。Windows向けの非コンソール（windowed）ビルドでは `sys.stdout`/`sys.stderr` が存在せず `None` になるため、内部で使用しているuvicornのログ設定がその前提で失敗していた。起動時に標準出力・標準エラー出力を破棄用のダミーに差し替えることで回避。これに伴い、デスクトップ版でログイン画面が表示されてしまう問題も併せて解消。

## [1.5.3] - 2026-09-07

### 修正

- デスクトップ版（Windows）で「Failed to start embedded python interpreter!」エラーが出て起動できない不具合を修正。内部バックエンドの実行ファイル形式を、実行のたびに一時フォルダへ自己展開する方式（onefile）から、展開済みのフォルダをそのまま実行する方式（onedir）に変更（ユーザー名に日本語などの非ASCII文字を含む環境で自己展開に失敗しやすいことが原因でした）。
- 上記の影響で内部バックエンドが起動できず、デスクトップ版なのにログイン画面が表示されてしまっていた問題も解消（デスクトップ版はもともとログイン不要で、バックエンドが正常に起動すれば自動的にスキップされます）。
- デスクトップ版をWindowsの「プログラムのアンインストールまたは変更」から削除する際に、時間がかかって完了しない場合がある不具合に対処。アプリ終了時に内部バックエンドのプロセスを確実に（プロセスツリーごと）終了するように変更し、ファイルがロックされたまま残ることを防止。インストール直後の自動起動も無効化し、正常に閉じられないまま次の操作に進んでしまう状況を減らした。

## [1.5.2] - 2026-09-07

### 修正

- デスクトップ版（Windows）のインストーラーが確認画面なしで即座にインストールされていた不具合を修正。保存先フォルダの確認・「次へ」・「インストール」のあるセットアップウィザード形式に変更。
- デスクトップ版のアプリアイコンが電子アプリの既定アイコンのままだった問題を修正。本アプリのロゴ（本＋しおり、青のグラデーション）に合わせた専用アイコンを追加。
- デスクトップ版で、インストール後にアプリを起動すると画面が真っ白のまま開かない不具合を修正。フロントエンドの読み込み方式を `file://` 直接読み込みから、アプリ専用のプロトコル経由の読み込みに変更（Chromiumの `file://` 上でのESモジュール読み込み制限が原因でした）。
- デスクトップ版で内部バックエンドの起動に失敗した場合、これまでは無反応・無表示のまま何も起きなかったが、エラー内容を示すダイアログを表示するように変更。

## [1.5.1] - 2026-09-07

### 修正

- 「記事」画面の「フォルダなし」が、未分類の記事が無いときでも常に表示され、削除もできなかった不具合を修正。未分類の記事があるときだけ表示するように変更。
- 「お気に入り」をフォルダ一覧から独立させ、「+」ボタンの隣の★アイコンから表示できるように変更。

## [1.5.0] - 2026-09-07

### 追加

- 本文に挿入した図（Mermaid図解／draw.io）・表・注意書き・カレンダー・展開ブロック・セクション区切りに、つまみをドラッグして位置を移動できる機能を追加。

## [1.4.0] - 2026-09-07

### 追加

- フォルダ・タグの名前変更・削除に対応（これまでは追加のみ可能でした）。
- 左サイドバーの「検索」を「記事」に、「整理」を「区分管理」に名称変更。「お気に入り」は独立した画面ではなく、記事一覧のツリー内のフィルタになりました。
- 「記事」画面をフォルダツリー表示に刷新。フォルダをクリックすると中の記事が展開表示され、右クリックでフォルダ・記事の編集/削除、「+」ボタンでその場からフォルダ・記事を新規作成できます。検索すると従来どおりの一覧表示に切り替わります。
- サイドバーの「+」ボタンから、記事だけでなくフォルダ・タグも新規作成できるように。
- 区分管理（フォルダ・タグ管理）画面でもフォルダ・タグの名前変更・削除が可能に。
- ヘルプをサイドバーの独立したアイコンに移動（設定画面のメンバー管理の下に埋もれていた問題を解消）。
- 設定画面のメンバー管理に、名前・メールアドレスでの検索機能を追加。
- 記事テンプレートを追加：計画表、議事録、手順書・マニュアル、ふりかえり（KPT）。

## [1.3.0] - 2026-09-07

### 追加

- 「+」挿入メニューに「フリー図形（draw.io）」を追加。draw.io（diagrams.net）本体をそのまま埋め込み、四角・線・矢印などの図形を自由に配置・接続して描ける。挿入後もdraw.ioで再編集可能。

## [1.2.0] - 2026-09-07

### 追加

- 「+」挿入メニューに「日時」を追加。カレンダーから選んだ日付を本文にバッジとして追加できる。
- 「+」挿入メニューに「展開」を追加。クリックすると内容が表示される折りたたみブロック（閲覧画面でもクリックで開閉可能）。
- カレンダーブロックの日付セルをクリックすると強調表示されるように（保存後も表示に反映される）。

## [1.1.1] - 2026-09-07

### 修正

- 管理者パネルの「メール設定」タブ・カードのアイコンが、存在しないアイコン名を指定していたため表示されていなかった不具合を修正。

### 追加

- メール設定に、通知の種類ごとの有効/無効スイッチを追加（アカウント登録通知／プロジェクト参加承認通知／パスワードリセット完了通知／記事作成通知）。メール機能全体を有効にした上で、種類ごとに送信するかどうかを選べる。
- 記事が作成されたときに、作成者以外のプロジェクトメンバーへ通知するメールを追加（メール機能・記事作成通知がともに有効な場合のみ送信）。

## [1.1.0] - 2026-09-07

### 追加

- 管理者パネルの「全ユーザー」「全プロジェクト」に検索機能を追加。
- 「全ユーザー」「全プロジェクト」でチェックボックスによる複数選択・一括削除に対応。
- パスワード／ユーザー名を忘れたユーザー向けのサポート窓口を追加。ログイン画面から「パスワードを忘れた場合」「ユーザー名を忘れた場合」を送信すると、管理者パネルの新しい「サポート依頼」タブに赤枠で強調表示される。
  - パスワードの場合：管理者が仮パスワードを発行できる。ユーザーが仮パスワードでログインすると、新しいパスワードの設定を求める画面が表示される。
  - ユーザー名（メールアドレス）の場合：本人確認情報を管理者が確認し、連絡後に「連絡済み」ボタンでクローズできる。
- 管理者パネルに「メール設定」タブを追加。SMTP設定を保存し、アクセス承認時の通知メールや、選択したユーザーへのお知らせメール送信に利用できる（初期状態は無効。設定確認用のテスト送信は無効時でも可能）。

## [1.0.3] - 2026-09-07

### 修正

- 管理者パネルのプロジェクト一覧で「管理」ボタンを押しても反応がないように見えた不具合を修正（詳細パネルが一覧の下に追加されるだけで、一覧が長いと画面外に隠れていた）。押すと必ずその場でダイアログが開くように変更。

### 追加

- 管理者パネルの画面構成を、全項目が縦に並ぶ1画面から「全般設定／ユーザー管理／プロジェクト管理」のタブ切り替え式レイアウトに変更し、操作しやすく整理。
- 管理者がプロジェクト名の変更・プロジェクトの完全削除（記事・フォルダ・タグ・コメントを含む）を行えるように。

## [1.0.2] - 2026-09-07

### 修正

- レート制限がリクエスト元IPとしてnginxのプロキシ内部IPを参照しており、実際にはアプリの全ユーザーがログイン試行回数制限を共有してしまっていた重大な不具合を修正（`X-Real-IP`ヘッダーを正しく参照するよう修正）。これにより、負荷のかかる操作の後に正規ユーザーがログインできなくなる問題を解消。

### 追加

- 管理者パネルに全ユーザー一覧を追加。ユーザーの追加・削除、CSVによる一括インポート（インポート用テンプレートのダウンロード付き）、CSVエクスポートに対応。
- ナレッジ作成時にテンプレートを選択できるように（白紙／日報／週報／報告書）。

## [1.0.1] - 2026-09-06

セキュリティ強化リリース。v1.0.0からアップデートを推奨します。

### セキュリティ

- 本文エディタのHTML直接挿入・Markdown埋め込み・記事本文の表示経路をサニタイズ（DOMPurify・サーバー側の独自サニタイザ）し、保存された悪意あるHTMLが他ユーザーの画面で実行されてしまう脆弱性を修正。
- 動画埋め込みURL・アップロード画像のファイル名がHTML属性へ未エスケープで挿入されていた箇所を修正。
- 管理者アカウントの初回設定（`/api/admin/setup`）に、サーバー起動ログに表示される初期設定トークンの入力を必須化（第三者による先取り設定を防止）。管理者パスワードの最小文字数を8文字に引き上げ。
- ログイン・新規登録・管理者ログイン・管理者初期設定にレート制限を追加。ログイン時のタイミング差によるメールアドレス存在確認（ユーザー列挙）を防止。
- セッション署名鍵およびローカル保存データ（認証情報を含む）のファイル権限を所有者のみに制限。
- `GET /api/projects`系のレスポンスから、非オーナーメンバーに不要な保存先の詳細設定を除去。
- ホストのファイルシステム参照機能（`/api/fs/*`）を、プロジェクト作成中のユーザーとオーナーのみに制限。

### 修正

- 本文に3つ目のリッチコンテンツ（表・注意書き・図解など）を挿入すると、カーソル位置の復元に失敗し何も挿入されない場合がある不具合を修正。

## [1.0.0] - 2026-09-06

初回リリース。

### 追加

- 記事の作成・検索・整理を1画面で行えるナレッジ管理アプリ本体（React + FastAPI）。
- 見出し・テーブル・注意書き・画像・動画・Mermaid図解（フローチャート／シーケンス図／状態遷移図／ER図など）に対応したリッチな本文エディタ。図はコードとビジュアルの両方から編集可能。
- 複数プロジェクト（ワークスペース）管理。プロジェクトごとに記事・フォルダ・タグを完全分離。
- ログイン機能、招待URLによるチームメンバーの承認制アクセス管理、複数オーナーによる共同管理。
- ローカルストレージに加え、AWS S3 / Google ドライブ / SharePoint Online / Box を保存先として選択可能。
- サーバー版向けの管理者パネル：初回起動時にアカウントを設定、全プロジェクト横断でのユーザー承認・削除・記事移動/削除、ログイン機能の有効/無効切り替え。
- Windows / Mac / Linux 向けデスクトップアプリ：ログイン不要のローカル動作。設定画面から任意でチームのサーバーに接続可能（接続テスト＋ログイン）。
- GitHub Pagesでの製品紹介ページ、Docker/プレーン両対応のセットアップ手順。
